using NUnit.Framework;
using parser;

namespace Tests
{
    public class Tests
    {
        Parser nmlParser;

        [SetUp]
        public void Setup()
        {
            nmlParser = new Parser();
        }

        [Test]
        public void TestParsing()
        {
            nmlParser
            Assert.Pass();
        }
    }
}